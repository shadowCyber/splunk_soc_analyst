[soc_alert_triage]
external_type = kvstore
collection = soc_alert_triage
fields_list = alert_id,status,owner,severity,risk_score,sla_due,last_updated,source,rule_name,entity,notes,tier_queue,escalated_by,escalated_at

[soc_triage_audit]
external_type = kvstore
collection = soc_triage_audit
fields_list = alert_id,action,actor,timestamp,before_status,after_status,comment

[soc_suppression_list]
external_type = kvstore
collection = soc_suppression_list
fields_list = _key,rule_name,entity_pattern,suppress_type,reason,created_by,created_at,expiry,active

[soc_asset_criticality]
external_type = kvstore
collection = soc_asset_criticality
fields_list = _key,entity,criticality,asset_type,asset_owner,business_unit,notes

[soc_user_risk]
external_type = kvstore
collection = soc_user_risk
fields_list = _key,user,risk_score,risk_level,risk_reasons,last_updated

[soc_threat_intel]
external_type = kvstore
collection = soc_threat_intel
fields_list = _key,ioc,ioc_type,category,confidence,source,expiry

[soc_analyst_roster]
external_type = kvstore
collection = soc_analyst_roster
fields_list = _key,username,display_name,email,ldap_group,splunk_role,tier,active,last_synced

[soc_runbooks]
external_type = kvstore
collection = soc_runbooks
fields_list = _key,title,category,severity_scope,tags,content_md,author,version,last_updated,active
